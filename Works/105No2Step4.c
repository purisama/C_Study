#include <stdio.h>
#include <conio.h>

int weight ,price = 30;
float rusult;

int main()
{
    printf("Enter the number of weight : ");
    scanf("%d",&weight);
    rusult = weight*price;
    printf("\nTotal cost : %.2f",result);
    getch();
    return 0;
}